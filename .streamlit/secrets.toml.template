# Streamlit Secrets Template
# Copy this file to .streamlit/secrets.toml and fill in your actual values
# NEVER commit .streamlit/secrets.toml to version control!

# OpenAI API Configuration
OPENAI_API_KEY = "sk-proj-YOUR_OPENAI_API_KEY_HERE"

# PostgreSQL/Supabase Database Configuration
POSTGRES_HOST = "your-postgres-host.supabase.com"
POSTGRES_PORT = 6543
POSTGRES_DBNAME = "postgres"
POSTGRES_USER = "postgres.your_user_id"
POSTGRES_PASSWORD = "your-database-password"
POSTGRES_SSLMODE = "require"

# Application Access Password
USER_KEY = "your-access-password"

# Databricks Configuration (if using)
DATABRICKS_SERVER_HOSTNAME = "your-workspace.cloud.databricks.com"
DATABRICKS_ACCESS_TOKEN = "your-databricks-token"
DATABRICKS_HTTP_PATH = "/sql/1.0/warehouses/your-warehouse-id"

# Optional: Vanna.AI Configuration (if using hosted service)
# VANNA_API_KEY = "your-vanna-api-key"
# VANNA_MODEL = "your-model-name"
