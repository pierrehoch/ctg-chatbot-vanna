# Render PostgreSQL Configuration
# 
# This file contains the configuration for connecting to your Render PostgreSQL database
# 
# ⚠️  IMPORTANT: NEVER commit this file to version control if it contains real credentials!
#
# To use this file:
# 1. Copy it as .env.local (for local development)
# 2. Fill in your actual Render database credentials
# 3. Load it with: eval "$(cat .env.local | xargs -I{} echo '{}')"
#    OR use python-dotenv: from dotenv import load_dotenv; load_dotenv('.env.local')

# ============================================================================
# RENDER DATABASE CREDENTIALS
# ============================================================================

# Your Render PostgreSQL hostname
# You can find this in your Render dashboard under Database Details
# Example: neon-database-xyz123.onrender.com
export RENDER_DB_HOST="your-render-hostname.onrender.com"

# PostgreSQL port (default is 5432 for most Render databases)
export RENDER_DB_PORT="5432"

# Your database name (usually 'postgres' or your custom database name)
export RENDER_DB_NAME="your_database_name"

# Your database user (usually 'postgres' or your custom user)
export RENDER_DB_USER="your_database_user"

# Your database password
# ⚠️  Keep this secure and never share it
export RENDER_DB_PASSWORD="your_secure_database_password"

# ============================================================================
# OPENAI API CONFIGURATION (Optional - only needed for embeddings)
# ============================================================================

# Your OpenAI API key for generating embeddings
# Get this from: https://platform.openai.com/account/api-keys
# Only required if GENERATE_EMBEDDINGS = True in the script
export OPENAI_API_KEY="sk-your-openai-api-key-here"

# ============================================================================
# OPTIONAL: APPLICATION LOGGING
# ============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
# export LOG_LEVEL="INFO"

# Enable file logging (true/false)
# export ENABLE_FILE_LOGGING="true"

# ============================================================================
# USAGE
# ============================================================================
#
# Load these variables before running the script:
#
# macOS/Linux:
#   source .env.local
#   python load_to_render_postgres.py
#
# Windows (PowerShell):
#   . .\.env.local  # First ensure execution policy allows it
#   python load_to_render_postgres.py
#
# Or with python-dotenv (recommended for Python scripts):
#   from dotenv import load_dotenv
#   load_dotenv('.env.local')

# ============================================================================
# FINDING YOUR RENDER CREDENTIALS
# ============================================================================
#
# 1. Log in to https://dashboard.render.com
# 2. Navigate to your PostgreSQL database
# 3. Click "Info" tab
# 4. Copy the connection details:
#    - Hostname (RENDER_DB_HOST)
#    - Port (RENDER_DB_PORT)
#    - Database (RENDER_DB_NAME)
#    - User (RENDER_DB_USER)
#    - Password (RENDER_DB_PASSWORD)
#
# Connection string format:
#   postgresql://user:password@hostname:port/database

# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# ✅ DO:
#   - Use strong, unique passwords
#   - Store credentials in environment variables
#   - Use .env.local or .env.production files
#   - Add .env.* files to .gitignore
#   - Rotate credentials regularly
#   - Use SSL/TLS connections (default with Render)
#
# ❌ DON'T:
#   - Commit .env files with credentials to Git
#   - Share credentials via email or chat
#   - Use the same password for multiple services
#   - Store credentials in code comments
#   - Log credentials in output files

# ============================================================================
# TESTING YOUR CONFIGURATION
# ============================================================================
#
# After setting these variables, test the connection:
#
# Python test:
#   python -c "from load_to_render_postgres import load_config_from_env; print('✅ Config loaded!'); load_config_from_env()"
#
# Command line test (requires psql):
#   psql -h $RENDER_DB_HOST -p $RENDER_DB_PORT -U $RENDER_DB_USER -d $RENDER_DB_NAME -c "SELECT 1;"
